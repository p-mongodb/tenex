h3 Patches

ul
  - @patches.each do |patch|
    li
      a href="/eg/#{@project.id}/versions/#{patch.version}"
        = patch.description

h3 Versions

ul
  - @versions.each do |version|
    li
      a href="/eg/#{@project.id}/versions/#{version.id}"
        = version.message
